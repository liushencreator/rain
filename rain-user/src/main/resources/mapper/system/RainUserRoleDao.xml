<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace必须指向Dao接口 -->
<mapper namespace="com.rao.dao.system.RainUserRoleDao">
	
	<resultMap id="RainUserRoleResultMap" type="com.rao.pojo.entity.system.RainUserRole">
		<id property="id" column="id"  />
		<result property="userId" column="user_id" />
		<result property="roleId" column="role_id" />
	</resultMap>
	
	<!-- 定义可重用的SQL代码段，可以包含在其他语句中 -->
	<sql id="base_column">
		 id ,user_id ,role_id 
	</sql>
	
	<sql id="base_value_column">
		 #{id},#{userId},#{roleId}
	</sql>
	
	<!-- 定义可重用的WHERE SQL公共代码段，可以包含在其他语句中 -->
	<sql id="where_column">
		<if test="id != null">
			AND id = #{id,jdbcType=BIGINT}
		</if>
		<if test="userId != null">
			AND user_id = #{userId,jdbcType=BIGINT}
		</if>
		<if test="roleId != null">
			AND role_id = #{roleId,jdbcType=BIGINT}
		</if>
	</sql>
	
	<!-- 设置语句 -->
	<sql id="set_column">
		<if test="id != null">
			id = #{id,jdbcType=BIGINT},
		</if>
		<if test="userId != null">
			user_id = #{userId,jdbcType=BIGINT},
		</if>
		<if test="roleId != null">
			role_id = #{roleId,jdbcType=BIGINT},
		</if>
	</sql>

	<!-- 根据ID查询 -->
	<select id="find" resultMap="RainUserRoleResultMap">
		SELECT	<include refid="base_column" />
		FROM	RAIN_USER_ROLE
		WHERE	ID=#{id,jdbcType=BIGINT}
	</select>
	
	<!-- 查询所有 -->
	<select id="findAll" resultMap="RainUserRoleResultMap">
		SELECT	<include refid="base_column" />
		FROM	RAIN_USER_ROLE
	</select>
	
	<!-- 统计记录数 -->
	<select id="count" resultType="int">
		SELECT COUNT(*) FROM RAIN_USER_ROLE
		<where> 
			<include refid="where_column" />
		</where>
	</select>
	
	<!-- 插入 -->
	<insert id="insert" parameterType="com.rao.pojo.entity.system.RainUserRole">
		<selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
			SELECT LAST_INSERT_ID()
		</selectKey>
		INSERT RAIN_USER_ROLE
				(<include refid="base_column" /> )
		VALUES	(<include refid="base_value_column" />)
	</insert>
	
	<!-- 修改 -->
	<update id="update" parameterType="com.rao.pojo.entity.system.RainUserRole">
		UPDATE	RAIN_USER_ROLE
		<set>
			<include refid="set_column" />
		</set>
		WHERE	ID = #{id,jdbcType=BIGINT}
	</update>
	
	<!-- 删除单条记录 -->
	<delete id="delete">
		DELETE	FROM RAIN_USER_ROLE
		WHERE	ID = #{id,jdbcType=BIGINT}
	</delete>
	
	<!-- 删除多条记录-->
	<delete id="deleteAll">
		DELETE	FROM RAIN_USER_ROLE
		WHERE	ID	IN
    	<foreach item="RainUserRoleId" index="index" collection="ids" open="(" separator="," close=")">
	  		#{RainUserRoleId} 
		</foreach>
	</delete>
	
	<!-- 根据参数查询列表-->
	<select id="findByParams" resultMap="RainUserRoleResultMap">
		SELECT	<include refid="base_column" />
		FROM 	RAIN_USER_ROLE
		<where> 1=1
			<include refid="where_column" />
		</where>
	</select>
	
	<!-- 分页查询-->
	<select id="findByPage" resultMap="RainUserRoleResultMap">
		SELECT
		<include refid="base_column" />
		FROM RAIN_USER_ROLE
		<where> 1=1
			<include refid="where_column" />
		</where>
	</select>

	<!-- insertSelective -->
	<insert id="insertSelective" parameterType="com.rao.pojo.entity.system.RainUserRole">
		<selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
			SELECT LAST_INSERT_ID()
		</selectKey>
		insert into RAIN_USER_ROLE
		<trim prefix="(" suffix=")" suffixOverrides=",">
				id,
				<if test="userId != null">
					user_id,
				</if>
				<if test="roleId != null">
					role_id,
				</if>
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides=",">
				#{id,jdbcType=BIGINT},
				<if test="userId != null">
					#{userId,jdbcType=BIGINT},
				</if>
				<if test="roleId != null">
					#{roleId,jdbcType=BIGINT},
				</if>
		</trim>
	</insert>
	
</mapper> 