<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace必须指向Dao接口 -->
<mapper namespace="com.rao.dao.system.TSystemRoleAclsDao">
	
	<resultMap id="TSystemRoleAclsResultMap" type="pojo.entity.system.TSystemRoleAcls">
		<result property="roleId" column="ROLE_ID" />
		<result property="aclId" column="ACL_ID" />
	</resultMap>
	
	<!-- 定义可重用的SQL代码段，可以包含在其他语句中 -->
	<sql id="base_column">
		 ROLE_ID ,ACL_ID 
	</sql>
	
	<sql id="base_value_column">
		 #{roleId},#{aclId}
	</sql>
	
	<!-- 定义可重用的WHERE SQL公共代码段，可以包含在其他语句中 -->
	<sql id="where_column">
		<if test="roleId != null">
			AND ROLE_ID = #{roleId,jdbcType=BIGINT}
		</if>
		<if test="aclId != null">
			AND ACL_ID = #{aclId,jdbcType=BIGINT}
		</if>
	</sql>
	
	<!-- 设置语句 -->
	<sql id="set_column">
		<if test="roleId != null">
			ROLE_ID = #{roleId,jdbcType=BIGINT},
		</if>
		<if test="aclId != null">
			ACL_ID = #{aclId,jdbcType=BIGINT},
		</if>
	</sql>

	<!-- 根据ID查询 -->
	<select id="find" resultMap="TSystemRoleAclsResultMap">
		SELECT	<include refid="base_column" />
		FROM	T_SYSTEM_ROLE_ACLS
		WHERE	ID=#{id,jdbcType=BIGINT}
	</select>
	
	<!-- 查询所有 -->
	<select id="findAll" resultMap="TSystemRoleAclsResultMap">
		SELECT	<include refid="base_column" />
		FROM	T_SYSTEM_ROLE_ACLS
	</select>
	
	<!-- 统计记录数 -->
	<select id="count" resultType="integer">
		SELECT COUNT(*) FROM T_SYSTEM_ROLE_ACLS
		<where> 
			<include refid="where_column" />
		</where>
	</select>
	
	<!-- 插入 -->
	<insert id="insert" parameterType="pojo.entity.system.TSystemRoleAcls">
		INSERT T_SYSTEM_ROLE_ACLS
				(<include refid="base_column" /> )
		VALUES	(<include refid="base_value_column" />)
	</insert>
	
	<!-- 修改 -->
	<update id="update" parameterType="pojo.entity.system.TSystemRoleAcls">
		UPDATE	T_SYSTEM_ROLE_ACLS
		<set>
			<include refid="set_column" />
		</set>
		WHERE	ID = #{id,jdbcType=BIGINT}
	</update>
	
	<!-- 删除单条记录 -->
	<delete id="delete">
		DELETE	FROM T_SYSTEM_ROLE_ACLS
		WHERE	ID = #{id,jdbcType=BIGINT}
	</delete>
	
	<!-- 删除多条记录-->
	<delete id="deleteAll">
		DELETE	FROM T_SYSTEM_ROLE_ACLS
		WHERE	ID	IN
    	<foreach item="TSystemRoleAclsId" index="index" collection="ids" open="(" separator="," close=")">
	  		#{TSystemRoleAclsId} 
		</foreach>
	</delete>
	
	<!-- 根据参数查询列表-->
	<select id="findByParams" resultMap="TSystemRoleAclsResultMap">
		SELECT	<include refid="base_column" />
		FROM 	T_SYSTEM_ROLE_ACLS
		<where> 1=1
			<include refid="where_column" />
		</where>
	</select>
	
	<!-- 分页查询-->
	<select id="findByPage" resultMap="TSystemRoleAclsResultMap">
		SELECT
		<include refid="base_column" />
		FROM T_SYSTEM_ROLE_ACLS
		<where> 1=1
			<include refid="where_column" />
		</where>
		<if test="orderByRule != null and orderByRule != ''">
			ORDER BY ${orderByRule}
		</if>
		limit #{pageBegin}, #{pageSize}
	</select>
	
</mapper> 