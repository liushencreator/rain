server:
  port: 8086
  servlet:
    context-path: /auth
    tomcat:
      uri-encoding: utf-8

spring:
  mvc:
    pathmatch:
      use-suffix-pattern: true
      
  # eureka 注册名称
  application:
    name: rain-auth
  jackson:
    date-format: yyyy-MM-dd
    time-zone: GMT+8

  # 数据源  
  datasource:
    username: root
    password: mysql
    driver-class-name: com.mysql.jdbc.Driver
    jdbc-url: jdbc:mysql://localhost:3306/rain_auth_dev
  
  # redis  
  redis:
    host: localhost
    port: 6379
    password: redis
    database: 0
    
# eureka    
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/

# 熔断器      
feign:
  hystrix:
    enabled: true